# PySpark for Spark programming (ensure the version is compatible with your cluster)
pyspark==3.1.2

# MLflow for tracking experiments and model logging
mlflow==1.21.0

# XGBoost: This version includes Spark integration (used for SparkXGBClassifier)
xgboost==1.5.0

# Azure Storage Blob for any storage operations (optional but often useful)
azure-storage-blob==12.9.0
protobuf==3.20.3
numpy==1.23.5